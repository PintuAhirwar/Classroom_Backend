services:
  - type: web
    name: taxhub-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn taxhub_backend.wsgi:application"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: taxhub_backend.settings
      - key: DEBUG
        value: False
